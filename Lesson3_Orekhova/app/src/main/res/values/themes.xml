<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Lesson3" parent="Theme.MaterialComponents.Light.NoActionBar">
        parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorPrimaryDark">@color/dark_yellow</item>
        <item name="android:textColor">@color/gray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondaryVariant">@color/light_yellow</item>
        <item name="android:textColorSecondary">@color/light_gray</item>
        <!-- Background color. -->
        <item name="colorOnBackground">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor"
            tools:targetApi="l">?attr/colorPrimaryDark</item>
    </style>

    <style name="Theme.Lesson3" parent="Base.Theme.Lesson3" />
</resources>